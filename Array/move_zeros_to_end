vector<int> moveZeros(int n, vector<int> a) {

    vector<int> ans, zeros;

    for (int i = 0; i < n; i++) {
        if (a[i] == 0)  zeros.push_back(0);
        else ans.push_back(a[i]);
    }

    for (int i = 0; i < zeros.size(); i++) {
        ans.push_back(0);
    }

    return ans;
}
